import { Badge, Card, Page } from '@/components/ui'; import { ActionButton } from '@/components/actions'; import { houstonStories } from '@/lib/data';
export default function Houston(){return <Page eyebrow="Local Pulse" title="Houston Top 10" subtitle="Only stories and conversations with real Houston relevance."><div className="grid grid-2">{houstonStories.map((s,i)=><Card key={s}><Badge tone={i<3?'orange':''}>#{i+1}</Badge><h2>{s}</h2><p className="muted">Houston angle: What does this mean for listeners’ daily lives, neighborhoods, money, or weekend plans?</p><ActionButton label="Add to Show" doneLabel="Added" storageKey={`houston-${i}`} /></Card>)}</div></Page>}
