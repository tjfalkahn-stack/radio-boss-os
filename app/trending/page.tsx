import { Badge, Card, Page } from '@/components/ui'; import { topStories } from '@/lib/data';
export default function Trending(){return <Page eyebrow="National + Culture" title="Trending Now" subtitle="Stories selected for conversation potential—not just headline value."><Card>{[...topStories,...topStories].map((s,i)=><div className="story" key={i}><div><Badge tone={i%3===0?'red':i%3===1?'gold':'blue'}>{s.category}</Badge><h3>{s.headline}</h3><p>{s.summary}</p><p className="muted"><strong>On-air hook:</strong> {s.angle}</p><p><strong>Ask listeners:</strong> {s.question}</p></div><div className="score">{Math.max(82,s.score-i)}</div></div>)}</Card></Page>}
