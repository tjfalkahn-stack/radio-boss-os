import { Badge, Card, Page } from '@/components/ui'; import { rundown } from '@/lib/data';
export default function Rundown(){return <Page eyebrow="Drag-and-Drop Plan" title="Show Rundown" subtitle="The live show clock with timing, status, notes, and clip markers."><Card><div className="list">{rundown.map((x,i)=><div className={`list-item segment ${x.status==='live'?'live':''}`} key={x.time}><strong>{x.time}</strong><div><h3>{x.title}</h3><div className="muted">{x.duration} · {x.detail}</div></div><div style={{display:'flex',gap:8}}><Badge tone={x.status==='next'?'gold':x.status==='live'?'red':'green'}>{x.status}</Badge><button className="button secondary">Move</button></div></div>)}</div></Card></Page>}
